# SonicWall Configuration Export - VPN WEAK ENCRYPTION
# This configuration focuses on VPN security issues
# Tests: Weak VPN encryption (DES, 3DES), PSK-only authentication

# System Settings
firmware version 7.0.1-5050
hostname "VPN-Weak-FW"
timezone "UTC"

# NTP Configuration
ntp-server 0.pool.ntp.org

# Interface Configuration
interface X0 zone WAN ip 203.0.113.1 netmask 255.255.255.0
interface X1 zone LAN ip 192.168.1.1 netmask 255.255.255.0

# Security Features - All enabled
ips enable
gateway-av enable
dpi-ssl enable
app-control enable
content-filter enable
botnet enable

# Admin Settings - Secure
admin username "security-admin"
mfa enable
https admin port 8443

# Address Objects
address-object name "Server1" ip 192.168.1.100 zone LAN

# Service Objects
service-object name "HTTP" protocol tcp port 80

# Firewall Access Rules
access-rule name "Allow-Outbound" from LAN to WAN source any destination any service any action allow comment "Normal outbound traffic"

# NAT Policies
nat-policy original-source 192.168.1.0/24 translated-source 203.0.113.1 interface X0

# VPN Configuration - MULTIPLE RISKS
# HIGH RISK: DES encryption (very weak)
vpn policy "Very-Weak-VPN-1" encryption des auth psk
vpn tunnel "Weak-Tunnel-1" encryption des authentication psk

# HIGH RISK: 3DES encryption (weak)
vpn policy "Weak-VPN-2" encryption 3des auth psk
vpn tunnel "Weak-Tunnel-2" encryption 3des authentication certificate

# MEDIUM RISK: PSK-only authentication (even with strong encryption)
vpn policy "PSK-Only-VPN-1" encryption aes256 auth psk
vpn policy "PSK-Only-VPN-2" encryption aes128 auth psk

# Secure VPN (for comparison)
vpn policy "Secure-VPN" encryption aes256 auth certificate
vpn tunnel "Secure-Tunnel" encryption aes256 authentication certificate

# End of Configuration
