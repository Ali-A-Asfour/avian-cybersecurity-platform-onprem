# SonicWall Configuration Export - BEST PRACTICE VIOLATIONS
# This configuration focuses on best practice issues
# Tests: Missing rule descriptions, outdated firmware, missing NTP

# System Settings
# MEDIUM RISK: Outdated firmware (more than 6 months old)
firmware version 6.5.0-1000
hostname "BestPractice-Violation-FW"
timezone "UTC"

# NO NTP CONFIGURED (LOW RISK)
# ntp-server 0.pool.ntp.org

# Interface Configuration
interface X0 zone WAN ip 203.0.113.1 netmask 255.255.255.0
interface X1 zone LAN ip 192.168.1.1 netmask 255.255.255.0

# Security Features - All enabled
ips enable
gateway-av enable
dpi-ssl enable
app-control enable
content-filter enable
botnet enable

# Admin Settings - Secure
admin username "security-admin"
mfa enable
https admin port 8443

# Address Objects
address-object name "Server1" ip 192.168.1.100 zone LAN
address-object name "Server2" ip 192.168.1.101 zone LAN

# Service Objects
service-object name "HTTP" protocol tcp port 80
service-object name "HTTPS" protocol tcp port 443

# Firewall Access Rules
# LOW RISK: Multiple rules without descriptions
access-rule name "Rule-1" from LAN to WAN source any destination any service HTTP action allow
access-rule name "Rule-2" from LAN to WAN source any destination any service HTTPS action allow
access-rule name "Rule-3" from LAN to DMZ source any destination any service any action allow
access-rule name "Rule-4" from DMZ to LAN source any destination any service any action deny
access-rule name "Rule-5" from LAN to WAN source Server1 destination any service any action allow

# One rule with description (for comparison)
access-rule name "Documented-Rule" from LAN to WAN source any destination any service any action allow comment "This rule has a description"

# NAT Policies
nat-policy original-source 192.168.1.0/24 translated-source 203.0.113.1 interface X0

# VPN Configuration
vpn policy "Site-VPN" encryption aes256 auth certificate

# End of Configuration
