# SonicWall Configuration Export - SECURITY FEATURES DISABLED
# This configuration focuses on disabled security features
# Tests: IPS, GAV, DPI-SSL, Botnet, AppControl, ContentFilter disabled

# System Settings
firmware version 7.0.1-5050
hostname "Security-Disabled-FW"
timezone "UTC"

# NTP Configuration
ntp-server 0.pool.ntp.org

# Interface Configuration
interface X0 zone WAN ip 203.0.113.1 netmask 255.255.255.0
interface X1 zone LAN ip 192.168.1.1 netmask 255.255.255.0

# Security Features - MULTIPLE DISABLED (CRITICAL/HIGH/MEDIUM RISKS)
# CRITICAL RISK: IPS disabled
ips disable

# CRITICAL RISK: Gateway Anti-Virus disabled
gateway-av disable

# MEDIUM RISK: DPI-SSL disabled
dpi-ssl disable

# HIGH RISK: Botnet Filter disabled
botnet disable

# MEDIUM RISK: Application Control disabled
app-control disable

# MEDIUM RISK: Content Filtering disabled
content-filter disable

# These are enabled
geo-ip enable
anti-spyware enable

# Admin Settings - Secure
admin username "security-admin"
mfa enable
https admin port 8443

# Address Objects
address-object name "Server1" ip 192.168.1.100 zone LAN

# Service Objects
service-object name "HTTP" protocol tcp port 80

# Firewall Access Rules
access-rule name "Allow-Outbound" from LAN to WAN source any destination any service any action allow comment "Normal outbound traffic"

# NAT Policies
nat-policy original-source 192.168.1.0/24 translated-source 203.0.113.1 interface X0

# VPN Configuration
vpn policy "Site-VPN" encryption aes256 auth certificate

# End of Configuration
